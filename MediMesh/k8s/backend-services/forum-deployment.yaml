# ════════════════════════════════════════════════════════════
# MediMesh — Forum Service Deployment
# ════════════════════════════════════════════════════════════
apiVersion: apps/v1
kind: Deployment
metadata:
  name: medimesh-forum
  namespace: medimesh
  labels:
    app: medimesh-forum
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: medimesh-forum
      tier: backend
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: medimesh-forum
        tier: backend
    spec:
      containers:
        - name: medimesh-forum
          image: bharath44623/medimesh_medimesh-forum:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 5009
              name: http
          env:
            - name: PORT
              value: "5009"
            - name: MONGO_URI
              value: "mongodb://medimesh-mongodb:27017/medimesh-forum-db"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: medimesh-secrets
                  key: JWT_SECRET
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: medimesh-config
                  key: NODE_ENV
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
